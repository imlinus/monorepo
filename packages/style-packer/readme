I love SCSS, but these days most of the good parts are built-in
However, I still want to bundle my files and write nestled styles

style-packer --input=test/style.css --output=dist/style.css

Optionally add
--watch
